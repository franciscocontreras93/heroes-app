import { ImageHeroePipe } from './image-heroe.pipe';

describe('ImageHeroePipe', () => {
  it('create an instance', () => {
    const pipe = new ImageHeroePipe();
    expect(pipe).toBeTruthy();
  });
});
